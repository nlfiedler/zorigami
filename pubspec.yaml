name: zorigami
description: Graphical interface for the zorigami application.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  equatable: ^1.1.1
  graphql: ^3.0.1-beta.3
  graphql_flutter: ^3.0.1-beta.3
  # oxidized: ^2.0.0
  oxidized:
    git: https://github.com/nlfiedler/oxidized.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  http: ^0.12.0+4
  mockito: ^4.1.1
  pedantic: ^1.8.0

flutter:
  uses-material-design: true
